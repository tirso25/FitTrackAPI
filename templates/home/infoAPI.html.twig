<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>FitTrackAPI Documentation 🔎</title>
    <link rel="icon" type="image/svg+xml" href="../assets/img/weighs.svg" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/highlight.js/11.7.0/styles/atom-one-dark.min.css">
    <style>
        @font-face {font-family: 'FiraCode'; src: url('../assets/fonts/FiraCode-VariableFont_wght.ttf') format('truetype'); font-weight: 100 900; font-display: swap;}
        body { background-color: #f8f9fa; padding-top: 2rem; font-family: 'FiraCode'}
        .api-card { margin-bottom: 2rem; border-radius: 0.5rem; box-shadow: 0 0.125rem 0.25rem rgba(0,0,0,0.075); }
        .json-container { background: #282c34; color: #abb2bf; border-radius: 0.5rem; padding: 1rem; }
        .endpoint { font-weight: bold; color: #61afef; }
        .method { font-weight: bold; color: #e06c75; }
    </style>
</head>
<body>
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-lg-10">
                <div class="card api-card">
                    <div class="card-header bg-primary text-white">
                        <h1 class="h4 mb-0">Welcome to FitTrackAPI! 👋</h1>
                    </div>
                    <div class="card-body">
                        <h2 class="h5 mb-3">API Endpoints Documentation</h2>
                        
                        {% for route, endpoints in apiInfo.info %}
                            <div class="mb-4">
                                <b><h3 class="h6 endpoint">{{ route }}</h3></b>
                                <ul class="list-group">
                                    {% for endpoint, description in endpoints %}
                                        <li class="list-group-item">
                                            <b><span class="method">{{ endpoint }}</span></b>: {{ description }}
                                        </li>
                                    {% endfor %}
                                </ul>
                            </div>
                        {% endfor %}
                    </div>
                </div>
                
                <div class="card api-card">
                    <div class="card-header bg-secondary text-white">
                        <h2 class="h5 mb-0">Raw JSON Response</h2>
                    </div>
                    <div class="card-body">
                        <pre class="json-container"><code>{{ jsonInfo }}</code></pre>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/highlight.js/11.7.0/highlight.min.js"></script>
    <script>hljs.highlightAll();</script>
</body>
</html>